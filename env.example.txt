# Copy this file to .env.local for development, or set these as Railway env vars in production.
# Rename to ".env.example" if you prefer (note: gitignore in this template excludes .env*).

# === Sunflower Land API ===
# Base URL of the Sunflower Land API.
#   Production: https://api.sunflower-land.com
#   Development: https://api-dev.sunflower-land.com
SFL_API_BASE_URL=https://api.sunflower-land.com

# Your x-api-key issued by Sunflower Land team
SFL_API_KEY=replace_me

# Your farm ID (the numeric ID of your in-game farm)
SFL_FARM_ID=0

# Optional: JWT if your endpoints require it (leave blank if using x-api-key only)
SFL_JWT=

# === Telegram Notifications ===
# Create a bot via @BotFather and paste the token here
TELEGRAM_BOT_TOKEN=replace_me
# Send any message to your bot, then visit
#   https://api.telegram.org/bot<TOKEN>/getUpdates
# to find your chat id (the "id" field under "chat")
TELEGRAM_CHAT_ID=replace_me

# === Scheduler ===
# How often (in minutes) to poll the farm state. Default 5.
POLL_INTERVAL_MINUTES=5
# Quiet hours: do not send notifications between these hours (24h, server local time).
# Leave both blank to disable.
QUIET_HOURS_START=
QUIET_HOURS_END=

# === Database ===
# Path to the SQLite file. In Railway, attach a volume and set this to a path
# inside the mounted volume so the DB survives redeploys:
#   DATABASE_PATH=/data/sfl.db   (volume mounted at /data)
DATABASE_PATH=./data/sfl.db

# === Timezone ===
# Used by node-cron for quiet hours and price-snapshot cron. Set to
# Asia/Jakarta for Master's time (UTC+7). Railway defaults to UTC.
TZ=Asia/Jakarta
